name: Compile ESP32 Binary

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Cache Arduino CLI
      uses: actions/cache@v3
      with:
        path: |
          ~/.arduino15
          ~/Arduino
        key: ${{ runner.os }}-arduino-${{ hashFiles('**/*.ino') }}
        restore-keys: |
          ${{ runner.os }}-arduino-
    
    - name: Install Arduino CLI
      run: |
        curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | sh
        echo "$PWD/bin" >> $GITHUB_PATH
    
    - name: Setup Arduino CLI Config
      run: |
        arduino-cli config init
        arduino-cli config add board_manager.additional_urls https://espressif.github.io/arduino-esp32/package_esp32_index.json
    
    - name: Update Core Index
      run: arduino-cli core update-index
    
    - name: Install ESP32 Core
      run: arduino-cli core install esp32:esp32@2.0.14
    
    - name: Install Libraries
      run: |
        arduino-cli lib install ArduinoJson
    
    - name: Compile Sketch
      run: |
        mkdir -p code
        mv gifi_blood.ino code/ 2>/dev/null || true
        arduino-cli compile --fqbn esp32:esp32:esp32 --output-dir build code/gifi_blood.ino
    
    - name: Upload Artifact
      uses: actions/upload-artifact@v3
      with:
        name: gifi-blood-firmware
        path: |
          build/*.bin
          build/*.elf
        retention-days: 30
